<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CadViewer.UIControls"
                    xmlns:cvt="clr-namespace:CadViewer.Converters"
                    xmlns:if="clr-namespace:CadViewer.Interfaces"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <DataTemplate DataType="{x:Type if:PropertyItemIntegerData}">
        <TextBlock Background="Wheat" Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </DataTemplate>

    <DataTemplate DataType="{x:Type if:PropertyItemStringData}">
        <TextBox Background="Yellow" Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </DataTemplate>

    <Style TargetType="{x:Type local:CPropertyTreeItem}" BasedOn="{StaticResource {x:Type ItemsControl}}">
        <Setter Property="Foreground" Value="{DynamicResource Cmm_TextColor}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CPropertyTreeItem}">
                    <Border BorderThickness="1" BorderBrush="#2e3440">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <!--Property name-->
                            <TextBlock Grid.Column="0" Width="50" VerticalAlignment="Center" Margin="5,0,5,0" Text="{Binding Name}" FontSize="12" />
                            <!--Property value-->
                            <ContentControl Grid.Column="1" Content="{Binding Value, Mode=TwoWay}" />
                            <ItemsControl ItemsSource="{Binding Children}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <local:CPropertyTreeItem DataContext="{Binding}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:CPropertyTree}" BasedOn="{StaticResource {x:Type ItemsControl}}">
        <Setter Property="Foreground" Value="{DynamicResource Cmm_TextColor}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CPropertyTree}">
                    <Border Background="#3b4453">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>