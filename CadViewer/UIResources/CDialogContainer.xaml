<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CadViewer.UIControls"
                    xmlns:cvt="clr-namespace:CadViewer.Converters"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Style TargetType="{x:Type local:CDialogContainer}" BasedOn="{StaticResource {x:Type ContentControl}}">
        <Setter Property="Foreground" Value="{DynamicResource Cmm_TextColor}" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="MinWidth" Value="300" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CDialogContainer}">
                    <Grid Background="Transparent">
                        <Border Margin="6,2,6,6" CornerRadius="5" BorderThickness="1">
                            <Border.Background>
                                <SolidColorBrush Color="{DynamicResource CompUI_Cmm_BackgroundColor}" />
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Color="#3b4453" />
                            </Border.BorderBrush>
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="100" Direction="0" ShadowDepth="0" Opacity="0.2" />
                            </Border.Effect>
                        </Border>
                        <Border Margin="6,2,6,6" Background="Transparent" >
                            <DockPanel>
                                <Border Margin="0" x:Name="PART_TitleDlg" Padding="5,2,1,0" Background="#3b4453" Height="24" CornerRadius="5,5,0,0" 
                                        DockPanel.Dock="Top" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                                    <DockPanel FlowDirection="LeftToRight">
                                        <StackPanel DockPanel.Dock="Right" FlowDirection="LeftToRight" Orientation="Horizontal">
                                            <Button Content="_" Width="20"></Button>
                                            <Button Content="X" Width="20"></Button>
                                        </StackPanel>
                                        <TextBlock Foreground="White" Text="Setting dialog"></TextBlock>
                                    </DockPanel>
                                </Border>
                                <Border Padding="10">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}" />
                                </Border>
                            </DockPanel>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!--<DataTrigger Binding="{Binding MessagePosition, RelativeSource={RelativeSource Self}}" Value="Top">
                            <Setter TargetName="PART_Canvas" Property="DockPanel.Dock" Value="Bottom" />
                            <Setter TargetName="PART_Message" Property="DockPanel.Dock" Value="Top" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessagePosition, RelativeSource={RelativeSource Self}}" Value="Bottom">
                            <Setter TargetName="PART_Canvas" Property="DockPanel.Dock" Value="Top" />
                            <Setter TargetName="PART_Message" Property="DockPanel.Dock" Value="Bottom" />
                        </DataTrigger>-->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>