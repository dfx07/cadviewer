<UserControl x:Class="CadViewer.Views.BackstageSaveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:CadViewer.Views"
             xmlns:vm="clr-namespace:CadViewer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid>
        <TextBlock Text="Save View Content" FontSize="16" Foreground="{DynamicResource Cmm_TextColor}" />
    </Grid>
</UserControl>
